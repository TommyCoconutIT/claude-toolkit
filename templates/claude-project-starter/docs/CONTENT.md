# TODO project name — Content

<!-- DELETE THIS FILE if your app generates content dynamically or has no seed content.

Keep it only if:
  - You're shipping a content-heavy app (course, catalog, marketing pages, lessons)
  - You have seed data that needs to be in the DB before launch
  - You want to version the content alongside the code

If you delete it, note the deletion in PROJECT_STATUS.md so future readers don't
look for it. -->

This file is the source of truth for all seed content. The seed script (TODO path) reads from this structure to populate the DB.

---

## Content Inventory

*High-level table of what exists.*

| Category | Count | Notes |
|---|---|---|
| TODO | TODO | TODO |

---

## TODO Section name

### TODO item name
*Per-item structure. Match what your seed script expects.*

| Field | Value | Notes |
|---|---|---|
| TODO | TODO | TODO |

*Repeat per item. Keep it scannable — long prose belongs elsewhere.*

---

## Seed Script Notes

*Where does the seed run? `npm run db:seed`? A SQL file? An admin upload?*

*Is it idempotent? Does it wipe content tables first? What does it leave alone (e.g., never touches user data)?*

*What's the diff workflow when content changes?*
